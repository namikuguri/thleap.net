box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: "HEAD"
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: thleap.net
        basedir: _public
